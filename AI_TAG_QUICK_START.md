# AI 标签推断功能 - 快速开始

## 5 分钟快速上手

### 第 1 步：打开设置

1. 启动应用
2. 点击设置按钮
3. 找到「翻译」tab 旁边的「AI 标签」tab

### 第 2 步：配置 API

#### 使用 DeepSeek（推荐，最便宜）

1. 点击「编辑配置」按钮
2. 在打开的文件中填写：

```json
{
  "enabled": true,
  "apiUrl": "https://api.deepseek.com/v1/chat/completions",
  "apiKey": "你的-deepseek-api-key",
  "model": "deepseek-chat",
  "maxTokens": 500,
  "temperature": 0.3,
  "minTagCount": 3
}
```

3. 保存文件

#### 获取 DeepSeek API Key

1. 访问 https://platform.deepseek.com/
2. 注册/登录账号
3. 进入 API Keys 页面
4. 创建新的 API Key
5. 复制 API Key 到配置文件

### 第 3 步：测试连接

1. 回到设置界面
2. 点击「测试 API 连接」按钮
3. 看到「API 测试成功」提示即可

### 第 4 步：查看标签统计（可选）

1. 点击「查看标签统计」按钮
2. 查看数据库中各类别标签数量
3. 了解 AI 会参考哪些标签

### 第 5 步：批量推断

1. 设置批量推断数量：10-20（建议从小数量开始）
2. 设置请求间隔：1000ms（避免 API 限流）
3. 点击「批量推断标签」按钮
4. 等待处理完成

**提示**：
- 只会处理状态为 `non-tag` 或 `tag-failed` 的书籍
- 可以随时点击「停止推断」中断处理
- 推断结果会实时显示在消息提示中

## 常见问题

### Q: 为什么推荐 DeepSeek？

A: 
- **价格**：约 ¥0.001/本，是 OpenAI 的 1/30
- **质量**：推断准确率与 GPT-3.5 相当
- **速度**：响应速度快
- **额度**：新用户有免费额度

### Q: 推断结果准确吗？

A: 
- 对于常见作品（如 FGO、蓝档、舰C）准确率很高（>90%）
- 对于小众作品可能需要人工复核
- 建议先用小批量测试，满意后再大批量处理

### Q: 会覆盖已有标签吗？

A: 
- **不会**！只处理 `non-tag` 或 `tag-failed` 状态的书籍
- 已经有标签的书籍会被跳过
- AI 推断的标签会与现有标签合并（不会删除）

### Q: 成本如何？

A: 
假设有 1000 本书需要推断：

| API 服务 | 单本成本 | 1000 本总成本 |
|---------|---------|--------------|
| DeepSeek | ¥0.001 | ¥1 |
| Qwen | ¥0.002 | ¥2 |
| OpenAI GPT-3.5 | $0.003 | $3 (约 ¥21) |
| 本地 Ollama | ¥0 | ¥0 |

### Q: API 限流怎么办？

A: 
- 增大「请求间隔」（如 2000ms）
- 减少「批量推断数量」（如 5-10）
- 使用本地 Ollama（无限流）

### Q: 如何使用本地 Ollama？

A: 
1. 安装 Ollama：https://ollama.ai/
2. 下载模型：`ollama pull qwen2.5:7b`
3. 配置文件改为：
```json
{
  "enabled": true,
  "apiUrl": "http://localhost:11434/v1/chat/completions",
  "apiKey": "ollama",
  "model": "qwen2.5:7b",
  "maxTokens": 500,
  "temperature": 0.3,
  "minTagCount": 3
}
```

### Q: 推断失败怎么办？

A: 
检查以下几点：
1. API Key 是否正确
2. 网络连接是否正常
3. API 额度是否用完
4. 请求间隔是否太短（被限流）

查看控制台日志获取详细错误信息。

## 最佳实践

### 1. 首次使用

- 先用 10 本书测试
- 检查推断结果质量
- 调整批量数量和间隔

### 2. 大批量处理

- 批量数量：10-20
- 请求间隔：1000-2000ms
- 分批处理，避免一次性处理太多

### 3. 成本控制

- 优先使用 DeepSeek（最便宜）
- 或使用本地 Ollama（免费）
- 避免重复推断（系统会自动跳过已标记的）

### 4. 质量保证

- 定期人工复核推断结果
- 对于重要作品，手动编辑标签
- 利用标签统计功能了解标签分布

## 下一步

- 阅读完整文档：`AI_TAG_README.md`
- 查看实现总结：`AI_TAG_FEATURE_SUMMARY.md`
- 运行测试脚本：`node tests/test_ai_tag_full.js`

## 技术支持

如有问题，请查看：
1. 控制台日志（F12 开发者工具）
2. 完整文档（`AI_TAG_README.md`）
3. GitHub Issues

祝使用愉快！🎉

